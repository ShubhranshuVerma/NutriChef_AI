# Development & testing tools (includes runtime deps)
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0   # coverage report: pytest --cov=app
pytest-html==4.2.0  # HTML test report: pytest --html=test-report.html --self-contained-html
