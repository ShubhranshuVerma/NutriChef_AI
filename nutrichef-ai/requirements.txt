# NutriChef AI — runtime dependencies (Python 3.11)
# Pinned for reproducible builds. Upgrade deliberately, then re-run tests.

# --- API ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
httpx==0.28.1

# --- Config / validation ---
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Database ---
sqlalchemy==2.0.54

# --- Data / ML ---
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.1
mlflow==3.16.1

# --- LLM / agents / RAG ---
langchain==1.4.1
langchain-core==1.6.3
langchain-google-genai==4.4.0
langgraph==1.2.11
langchain-chroma==1.1.0
chromadb==1.5.9
langchain-huggingface==1.2.2
sentence-transformers==6.0.1   # pulls in PyTorch (uses Apple-Silicon GPU via MPS)

# --- Frontend ---
streamlit==1.64.0

# --- Security ---
bcrypt==5.0.0
pyjwt==2.14.0
